# task19 技术批判(A 批重塑后整体形态,毒舌口径,URL 已机验)

> 批判对象:A 批(task02-18)重塑后的整体形态——演示可交付度 / 双端体验 / 遗留债。实证基础=task19 收口验收当日实测(2026-09-12):check-demo 绿 5/8、双线 E2E 22 PASS+1 ENV_BLOCKED、CDP 渲染矩阵 15/15、GET /api/series/1 两次实测 16266ms/8128ms。
> 批判者人设:以竞品为唯一标尺故意挑刺;A 批"接线完成"不等于"可交付达标",下述 4 条均为当日实测支撑,非口头预言。

| # | 批判点 | 竞品对标 | 差距 | 优化方案 | 最小验证 | 收益/成本 | 级别 |
|---|---|---|---|---|---|---|---|
| T19-1 | 演示可交付度仍被"VMware+Docker 双优先级依赖"绑架:check-demo 当日实测绿 5/8(Milvus/Mongo/Redis 全红),管理线 E2E 唯一 ENV_BLOCKED 即知识库分区;A 批只交付了检查单(C16 的 A 部分),存储五件套收敛为 2 件的 C 阶段里程碑零动作,知识库/RAG/检索整条演示线在 VM 关机时全灭,断链兜底只有"报红指引"没有"降级可用" | Supabase 用单 Postgres 同时承载业务数据与向量检索的生产路径(https://supabase.com/blog/openai-embeddings-postgres-vector ,2026-09-12);Redis 官方单实例开发部署即够演示场景(https://redis.io/docs/latest/develop/ ,2026-09-12) | 竞品默认路径=一个 PG 容器 compose up 撑全场景;我们演示前要拉 VMware 虚拟机+Docker Desktop+容器三件套,任一未开机即 3/8 红,演示可用性=祈祷开机率相乘 | C 阶段 pgvector 收敛占位立即给出最小验证脚本(2629×2048 双引擎 P95/召回对比,预期同量级即退役 Milvus);短期给检索线加 BM25/空态降级开关,Milvus 断链时 chat/RAG 页给诚实降级而非报错 | 跑通 pgvector 2629×2048 对比脚本;关 VM 后 chat 页应出降级文案而非异常 | 演示可用性从"VM 祈祷"变 compose up;成本=迁移一个周末+降级开关半天 | P1 |
| T19-2 | 学生端核心读路径性能不达演示底线:GET /api/series/1 当日两次实测 8.1s/16.3s,courses 列表单请求 ~2s,gamification/rankings 4~8s;CDP 渲染下 learning 页会话区标题轮询 24s 仍停留"加载中…"。"点开课程详情"是演示第一动作,冷场 10 秒级直接毁节奏;Redis 基建就在手边(task39 缓存已实证 322.6ms→5.3ms)却没用在系列详情读路径上 | HTTP Archive 全网 TTFB/加载指标基线报告(https://httparchive.org/reports/state-of-the-web ,2026-09-12);MDN 性能优化与响应预算指引(https://developer.mozilla.org/en-US/docs/Web/Performance ,2026-09-12) | 竞品生态把交互响应 <1s 当默认底线;我们详情接口 P95 8s+ 且无缓存层,等于用最慢路径演示最强卖点 | 定位 series/1 慢查询(explain/N+1)→套用既有 task39 Redis 读缓存模式;列表/详情接口加 P95 埋点进 check-demo;前端详情页补骨架屏把感知等待拆给 loading 态 | series/1 加缓存后压测 P95<500ms;无缓存时 explain 报告定位慢因 | 演示节奏保住/复用现成 Redis 基建约半天 | P1 |
| T19-3 | 错误契约泄漏:Milvus 断链时 GET /api/knowledge/partitions 返回 HTTP 500 + code 50000,message 直带内部异常串"MilvusException: Fail connecting to server on 192.168.85.101:19530"——内网拓扑(组件名+IP+端口)漏给任何登录用户;且 50000 兜底把"依赖不可用"与"代码缺陷"混进同一壳,前端无法按语义给降级文案,error_codes 表无依赖不可用类目 | Stripe 错误模型:类型化 error.code/message 面向用户、内部细节走日志(https://docs.stripe.com/api/errors ,2026-09-12);Zalando REST API 指南:problem+json、5xx 不暴露内部实现、用 503 表达依赖不可用(https://opensource.zalando.com/restful-api-guidelines/ ,2026-09-12) | 竞品把错误当 API 契约的一部分逐码登记;我们 50000 兜底一行串接异常字符串,把调试信息当用户文案 | error_codes 增"依赖不可用"类目(如 50301 DEPENDENCY_UNAVAILABLE→HTTP 503),message 脱敏为"知识库服务暂不可用,请稍后重试",内部 detail 只进日志;管理端知识库页按 503 给降级横幅 | 停 Milvus 调 /api/knowledge/partitions 应 503+脱敏 message;补一条 pytest 契约用例锁行为 | 安全合规+演示态诚实降级;成本约半天 | P1 |
| T19-4 | A 批 15 页全静态 IIFE 接线收口,但形态债未还:admin 仪表盘实测半幅是"契约缺口占位"(订单数/营收/热门课程榜无管理端聚合端点,页面自注待 task70~91),"平台运营总览"只画了半张图;数据层仍每页手写 loading/轮询,B0 选型审计与 B3 窄楔子在 A 批收口日零进展——若 B 批再延期,C15"双前端结构性浪费"将二次证伪 | Refine 契约→data Provider→组件一次装配全页复用(https://refine.dev/docs/ ,2026-09-12);react-admin 企业级 CRUD 与数据层抽象(https://marmelab.com/react-admin/documentation.html ,2026-09-12) | 竞品一次抽象全页复用、聚合端点随 CRUD 免费派生;我们 15 页各自接线+仪表盘占位等后端,结构成本已付两次(原型+接线),第三次(B 批组件化)还在计划里 | 立即启动 B0 审计(与 A 收口并行的计划已批,勿再拖);B0 DEMO spike 必须含 dataProvider 壳解包+分页映射的隐藏工作量;admin 聚合端点(订单/营收/热门课程)立变更单进 B 契约 | B3 admin-users Refine 版代码量<静态页 1/3(可证伪);B0 审计文档含 spike 代码量对照表 | 维护成本减半量级;B0 审计约 1 天 | P2 |

## 结论
四条全部实证支撑(T19-1/2/3 为当日 HTTP 实测, T19-4 为渲染矩阵+页面自注佐证),已由 review-gate 机验 URL 后自动登记 tracker;承接动作见 task19-优化修改方案.md。A 批判 DONE 与否见 test-reports/task19-acceptance-report.md 综合判定。
