# W1 优化修改方案（对应 W1-技术批判.md，2026-09-02）

| 批判 | 可落地修改 | 量化指标 | 测试方案 | 落点任务 | 风险 |
|---|---|---|---|---|---|
| 1. 401 静默刷新缺失 | edu-api.js 加 single-flight refresh：401→refresh 换新→重放一次→失败才 logout；刷新期间并发请求排队等待 | access 过期场景跳登录率 100%→0%（仅 refresh 也失效才跳） | 人为调短 token 过期实测；并发 5 请求同时 401 只发 1 次 refresh | tracker 待派（建议独立小任务或并入 task122） | refresh 端点行为需先 curl 实测（rotation 与否）；死循环防护：重放仅 1 次 |
| 2. 守卫 8 份拷贝 | 抽 `public/edu-guard.js`（三段守卫 + bootAdmin 调度），8 页改为一行引入 | 8 页守卫内联代码 grep=0；edu-guard.js 引用=8 | 三角色行为回归 + DEBUG 态/无效 token 两绕法复测 | task122 | 抽离时注意加载顺序在注入脚本之前 |
| 3. 死链扫描未进门禁 | scan-deadlinks.mjs 加入 task123 检查单 + L4 回归固定条目；头注释声明静态盲区口径 | W4 里程碑检查单含此项且跑通 1 次 | task123 执行时输出 0 死链 | task123 | 无 |
| 4. 注册两步式 | 后端契约增强评估：register 返回 token（或登录态建立端点）并入 C-A 契约单讨论 | 若采纳：注册→进入学习端 1 跳 | curl register 响应含 access_token | C-A（task114）讨论项，不强加 | 改注册响应契约涉及安全评审（自动登录 session fixation），默认不采纳，留档即可 |
