# C5 技术批判（C 阶段最小部署包整体，毒舌口径，URL 已机验）

> 批判对象：**C 阶段最小部署包整体**（taskC0~C4 交付物：`.env.example` 220 键模板 + `deploy.mjs` 三件套 + DEBUG=False 端到端 + `deploy/README.md` runbook），实证基础=C5 终验收当日实测（2026-09-13）：干净进程环境（8000/3000 清零）单命令 `node deploy.mjs start` → check-demo 绿 8/8（含双账号登录链路）+ 8 核心页 CDP 渲染 console 零错；抽验 `admin-users-refine-proto.html` 页 200（/api/admin/users 列表 API code=0）、`dashboard.html` 200、`/media/videos/VID-20260913-A3DA351A.mp4` HEAD 200（video/mp4 + accept-ranges: bytes）。
> 批判者人设：以竞品为唯一标尺故意挑刺；"本机同构一条命令 8/8"只是**开发机自举**，离"可交付给另一台机器的部署包"还有代差，下述 5 条均当日实测/源码核实支撑，非口头预言。

| # | 批判点 | 竞品对标 | 差距 | 优化方案 | 最小验证 | 收益/成本 | 级别 |
|---|---|---|---|---|---|---|---|
| C5-1 | "一键"只覆盖**服务进程编排**，不覆盖**依赖自举**：start 前置检查对 MySQL/VM Milvus/Mongo/Redis 只探测给指引不拉起（VM 电源/Docker Desktop/Redis 容器均脚本能力外），venv、node_modules、edu 库快照恢复（106 表）全靠新人手工按 README §3 走；换一台干净 Windows 机照 runbook 从零到 8/8 的真实耗时从未实测——README 标注两处"待验证"（venv 安装命令历史未统一留存、快照二是否含建库语句） | Coolify 官方安装文档给出一条命令安装（https://coolify.io/docs/get-started/installation/ ,2026-09-13）：`curl -fsSL` 下载 install.sh 管道给 bash，连 Docker 都代装、装完直接给 dashboard+管理员+密钥；Dokku bootstrap.sh 自动装依赖、装完 git push 即部署（https://dokku.com/docs/getting-started/installation/ ,2026-09-13） | 竞品"一键"=零依赖起点到可用控制台全自动；我们"一键"=**八项手工前置之后**的一键，且首次部署路径（README ③ 快照恢复 997MB dump）无计时无演练报告，新人卡死点不可预测 | ① 给 deploy.mjs 加 `doctor` 子命令：把 README §2/§3 全部前置（venv 存在性/node_modules/BUILD_ID/edu 库表计数/快照文件存在性）逐项机检并输出通过/缺省清单；② 新机器按 README 全流程走一遍并计时，把两处"待验证"补成实测结论落回 README | 在干净目录 `node deploy.mjs doctor` 应逐项报"缺 venv/缺 node_modules/edu 库 0 表"并给处置；README 全流程计时报告一份 | 新人首次部署成功率从"祈祷"变"逐项机检"；doctor 约半天+走查半天 | P1 |
| C5-2 | start"读 .env 形态原样、不做替换"是优点也是漏洞：**无形态断言门**——.env 是 dev 形态（DEBUG=true）时 start 照常拉起全栈且 exit 0（本次实测 WARN 后 8/8 全绿），一个不知情的运维会把 dev 形态当成"部署成功"交差；十二因子要求 build/release/run 三阶段分离、release 阶段固化 config，我们 release 阶段根本不存在，配置正确性全押在"用户自己读过 README §2"上 | Twelve-Factor App：III Config 配置存环境且同一 build 可跨环境提升、Build/Release/Run 严格分离（release=build+config）（https://12factor.net/ ,2026-09-13） | 竞品把"配置合法性"做成部署流水线的一道硬门；我们把门做在了**后端启动时**（_security_guard/P1-8 只在 DEBUG=false 才硬校验密钥），dev 形态下门全开而 deploy 脚本只 WARN 不拦截 | deploy.mjs start 增加 `--profile prod|dev` 显式参数：`--profile prod` 时断言 .env 满足 DEBUG=false+ENV_NAME=prod+密钥非默认+CORS 双源，不满足即 exit 2 拒绝拉起（不替改，只断言）；无参数维持现状（幂等兼容）；C3 生产验收脚本即复用此门 | 构造 DEBUG=true 的 .env 跑 `start --profile prod`，应拒绝启动并逐键报差异；改 prod 形态后应放行 | 消灭"dev 形态当生产交付"的人因事故；约 2 小时 | P1 |
| C5-3 | 进程治理是"裸 spawn"级：后端 uvicorn/前端 next start 均 detached spawn 后无人看护——进程崩溃不自愈、宿主重启后不自启（README 巡检第 4 条自己承认"Redis 未配开机自启待验证"）；stop 用 `taskkill /F /T` 强杀无优雅关闭窗口（in-flight SSE 请求直接断）；deploy-*.log 为追加式无轮转无上限，与 12-factor"日志=事件流交给执行环境"背道而驰 | Twelve-Factor App：IX Disposability 快启+优雅关闭、VI Processes 无状态可随意杀/重启、XI Logs 输出事件流由运行环境路由（https://12factor.net/ ,2026-09-13） | 竞品生态（compose restart 策略/PaaS 进程管理器）默认带守护与优雅停机；我们连"后端挂了谁能发现"都要靠人跑 status，日志三件套会随演示次数无界增长 | ① start 拉起后登记 PID+做一次性健康复查循环（可选 `--watch` 简单守护，崩溃重拉上限 N 次并写事件日志）；② stop 对后端先试 SIGTERM（uvicorn 优雅退出）再 taskkill /F 兜底；③ deploy-*.log 加启动时尺寸检查（>50MB 轮转为 .1 文件）；④ README 巡检第 4 条补实测结论（Redis restart 策略 `docker inspect --format '{{.HostConfig.RestartPolicy.Name}}'`） | kill -9 后端 PID，`--watch` 应重拉并记录事件；stop 后确认 uvicorn 退出码非 9 强杀路径；日志轮转触发一次 | 崩溃自愈+日志有界，运维面从"裸奔"到"可看护"；合计约 1 天 | P1 |
| C5-4 | 安全清单有"点"无"面"：_security_guard（密钥默认值拒启）、P1-8 门禁、无 token 401、X-Force 生产无效均为 C3 实测的**单点断言**，但没有一个可重复执行的生产安全自检命令——Django 有 `manage.py check --deploy` 一条命令扫全部部署安全项，我们 check-demo ⑧ 只测一个端点一个路径；且 JWT_SECRET 无轮换概念（轮换=全员 token 立即失效，无 fallback 窗口）、错误上报缺位（500 只进本地日志，无 ADMINS/Sentry 等价物，线上炸了没人知道）；/api/metrics 裸端点无鉴权护栏仍未收口（README 附录 task123 自检登记） | Django 官方部署检查单：check --deploy 自动化扫描 + SECRET_KEY 轮换配 SECRET_KEY_FALLBACKS 平滑过渡 + 错误上报 ADMINS/Sentry（https://docs.djangoproject.com/en/5.0/howto/deployment/checklist/ ,2026-09-13） | 竞品把部署安全做成**一条命令可重复跑的检查单+轮换机制+错误上报通道**三件套；我们三件全缺，安全态=最后一次人工验收的快照，之后无人保证 | ① deploy.mjs 加 `security-check` 子命令：聚合无 token 401/垃圾 token 401/X-Force 无效/密钥非默认/metrics 鉴权/50301 脱敏六断言（直接复用 C3_api_assert.py 与 README 附录检查单）；② JWT_SECRET_FALLBACKS 支持（双密钥校验窗口）；③ 错误上报最小形态：500 事件写独立 error-stream 日志+可选 webhook | `security-check` 在 prod 形态一次跑全绿、人为把 X-Force 改回 DEBUG 生效态时应红；fallback 窗口内旧 token 可用 | 生产安全态可日常复验（不只交付那一刻）；合计约 1 天 | P1 |
| C5-5 | 部署包"最小"的代价是**组件面最宽**：演示一条链路要 MySQL(宿主)+Redis(Docker)+Milvus/Mongo/MinIO/Neo4j(VM)+uvicorn+next 共 8 个运行件，任一不可达在 DEBUG=false 下**整体拒启**（C0 §5 实证）；附录 docker-compose 资产（MySQL+MinIO+etcd+Milvus+Mongo+Neo4j+前后端）存在但从未端到端验证，且 compose 路线与 deploy.mjs 路线是两套互不相认的事实源 | Supabase 自托管官方推荐路径=单 docker compose 拉起全栈、.env 一个文件管全部（https://supabase.com/docs/guides/self-hosting ,2026-09-13）；本机同构边界内 compose 也应是把"VM 拓扑"收敛为"一个 compose 文件"的正解 | 竞品自托管=一个 compose up；我们=VM 开机祈祷+两套部署事实源（runbook 主推 deploy.mjs，附录 compose 是"未验证的原样保留"），新人不知道哪条是正路 | ① 附录头部加"未端到端验证"显著横幅+与 deploy.mjs 路线的适用场景对照表（当前唯一正路=deploy.mjs）；② C 阶段全量把 compose 路线验证收口（healthcheck+种子导入+单命令），届时 VM 四件套进 compose 即可退役 VMware 拓扑 | compose 栈在一台有 Docker 的机器 up 后 /health/detail 全 ok + check-demo 8/8（含⑦关键页）；对照表落 README 附录 | 部署面从 8 件收敛为 1 个 compose；验证约 1~2 天（C 全量范围） | P2 |

## 结论

五条全部实证支撑（C5-1/C5-2 为当日单命令实测+脚本源码核实，C5-3/C5-4 为 deploy.mjs/README/check-demo 源码+README"待验证"自注核实，C5-5 为 C0/C1/C4 交付物交叉核实），已由 review-gate 机验 URL 后自动登记 tracker；承接动作见 C5-优化修改方案.md。终验收判定与遗留清单见 test-reports/C5-final-acceptance.md。
