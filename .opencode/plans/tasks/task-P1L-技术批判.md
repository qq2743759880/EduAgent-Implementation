# task-P1L 验收批判（强制技术批判）

> 对象：task-P1L LLM 档位延迟治理（Trae，commit e123c60→13f2972→7776a78→b4cf306→2d43706）
> 结论：**验收通过**（按用户 2d43706 验收口径裁定：以流式为准）。

## 实证结果
- commit 链完整（优化 H 5 commit + 周末复测 b4cf306 + 验收裁定 2d43706）。
- 优化 H：规则路由先行/knowledge 直连/子代理预执行/流式规则决策——链路 L2 5→2、L1 2→1。
- 契约 45 passed（P1L 28 + task92 6 + task_a1 11）。
- wknd4 周末正式轮：0 失败/0 降级/0 限流；**流式 L1 端到端 P95 6.4s 达标（≤8s）**；TTFT P50 L1 6.9s/L2 7.3s（较白天 -64%/-68%）。
- **用户裁定（2d43706）**：验收以流式为准（产品实际交互即流式）；非流式 P95 归因推理模型完整生成硬约束（可选优化 fast 模型/max_tokens 未实施）。
- sidecar 雪崩根因链完整定位 + RERANK_HTTP_TIMEOUT 2.0→10.0 缓解（config 注释详实）。

## 批判 1（P2）：非流式 P95 仍超标（L1 35s/L2-learning 44s），仅以"流式为准"口径通过
- **问题**：非流式 P95 未达标；用户裁定流式为准，但非流式接口仍暴露 35-44s 延迟。
- **方案**：可选优化（answer 改 fast 模型 / LLM_MAX_TOKENS 2000→800）——列入后续，避免非流式调用方（如有）体验劣化。

## 批判 2（P2）：TTFT P95 26~37s vs P50 6.9~7.3s，尾部波动大（样本 56）
- **问题**：TTFT 尾部 P95 未达 ≤3s 目标（P50 已近上限 6.9s）；样本量 56 尾部波动。
- **方案**：评估决策类调用（route/子代理 turn0）改非推理轻量模型（v3 系列）；扩大样本复测。

## 批判 3（P1，架构）：vector.py 云模式仍硬加载 BGE-M3（EMBED_BACKEND=cloud 下）
- **问题**：雪崩链根因——cloud 模式仍加载本地 BGE-M3 2.2GB CUDA，触发 GPU 竞争。
- **方案**：架构级根治——云模式跳过本地模型探测 + 进程内 reranker 回退改异步（报告 §8-P2 新增，待实施）。

## 批判 4（P2）：sidecar 超时雪崩已缓解（10.0s）但未根治
- **问题**：RERANK_HTTP_TIMEOUT 10.0s 缓解，进程内 reranker 同步阻塞 47s 仍可能（低概率）。
- **方案**：进程内回退改异步（与批判③同源）。

**结论**：批判①②为后续优化（fast 模型/max_tokens/轻量决策模型），③④为架构级（转专项）；task-P1L 按用户口径验收通过。
> 🔻 **2026-08-29 用户裁定：非流式优化降为低优先级已知遗留**
> - 实证：前端 3 个聊天组件全用流式（useChatStream），sendChat（非流式）=0 处调用 → 非流式 API 无 UI 消费方
> - 非流式 P95 35-44s / TTFT P95 26-37s 不影响实际用户体验（流式 L1 P95 6.4s 已达标）
> - 非流式保留为外部集成 API，延迟受推理模型完整生成硬约束；若有外部方调用再做 fast/max_tokens 优化
