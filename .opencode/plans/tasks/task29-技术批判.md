# task29 验收批判（强制技术批判）

> 对象：task29 AI 助手评估（Trae，commit c59bf51）
> 结论：**验收通过**（报告诚实披露完整，评估真实运行）。

## 实证结果
- commit `c59bf51`（12 文件 +2847/-13）；scripts/eval/ 8 模块 + task29_results.json 交付。
- 报告实证：评估集 40 条真实回放；GWT①路由 62.50%≥基线 57.50%✅、L0 误判 L3 0%✅（但 L0→L1+ 过度升档 100% 已诚实披露）；GWT②压测 P95 超标❌如实上报、成本 ¥529.64>¥300❌；GWT③缓存命中 96.11%✅；GWT④R8 keep_sixnode✅。
- 环境披露完整：Redis 不可达降级、Milvus 本次可达（edu_knowledge 4981 条）、CUDA GPU 可用（RTX 4060）、DeepSeek 402 余额中断后采用最后一次完整成功运行数值（未新增 LLM 调用）。

## 批判 1（P1）：L0 过度升档 L1+ 100%（chitchat 全量误路由）——路由质量缺口
- **问题**：chitchat 10/10 被升档到全检索，承担 L1 延迟与成本，chitchat 召回仅 20%。路由 prompt 判别力不足。
- **方案**：task29 之后路由优化专项（后续 AI 主线），补 L0 精准档位判定，避免闲聊烧钱。

## 批判 2（P1）：P95 严重超标（L1 87s/L2 66s/L3 107s vs 目标 8s）
- **问题**：fan_out 多轮 LLM + Redis 不可达 0.5s×N + DeepSeek 外部排队叠加。
- **方案**：task39 压测专项治理（Redis 恢复/并发削峰/降级优化）。

## 批判 3（P1）：成本超预算（¥529.64 > ¥300，L1 占 ¥370）
- **问题**：L1 21000 请求/月 + 全检索档成本高企；L0 未压测致成本低估。
- **方案**：结合批判①修路由（L0 不升档）+ 缓存命中实际落地（task27 前缀<1024 未达缓存门槛）。

## 批判 4（P2）：三层记忆向量库仍用哈希降级，未启用真实 Milvus+BGE-M3 CUDA
- **问题**：本次评估 Milvus 可达、CUDA 可用，但 MemoryVectorStore 默认 DeterministicEmbedder 哈希，user_memory 集合 0 行，记忆召回未用真实语义向量。
- **方案**：用户已提诉求，单列「向量检索 CUDA/Milvus 启用」专项任务（见看板 task-VEC）。

**结论**：task29 本体验收通过；批判①~③ 转后续专项，批判④ 单列 task-VEC 立即处理。