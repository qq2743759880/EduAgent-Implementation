========================================================================
task39 GWT② 容灾演练（连接层故障注入，不 kill 共享服务）
场景: baseline, mongo, minio
========================================================================

────────────────────────────────────────────────────────────────────────
■ 场景 baseline：基线对照（全依赖在线，不注入故障）
  注入: {}
  期望: 稳态：所有探针 2xx，且 RAG/问答无降级原因
    健康检查             HTTP 200        11ms  degraded=—
    详细健康             HTTP 200     15147ms  degraded=—
    课程列表（MySQL）      HTTP 200      2166ms  degraded=—
    RAG 检索（Milvus+图谱） HTTP 200      5421ms  degraded=有 | Milvus 检索跳过（MilvusException）；Neo4j 未连接（跳过图谱扩展）
    AI 问答（agent 全链路） HTTP 200     52015ms  degraded=—
  判定 无5xx: ✅   edu_degraded_total{component="baseline"} = 0.0
  ⚠️ baseline: edu_degraded_total 未增长（该场景可能无读路径探针命中，或该依赖本身有兜底不产生降级计数）
  ⚠️ 基线出现降级原因：稳态假设不成立，断连场景的降级对照需谨慎解读
  降级原因样本: ['RAG 检索（Milvus+图谱）: Milvus 检索跳过（MilvusException）；Neo4j 未连接（跳过图谱扩展）']

────────────────────────────────────────────────────────────────────────
■ 场景 mongo：MongoDB 对话状态断连
  注入: {'MONGO_URI': 'mongodb://127.0.0.1:65533'}
  期望: chat_message(MySQL) 权威历史兜底，不 500
    健康检查             HTTP 200        34ms  degraded=—
    详细健康             HTTP 200     15196ms  degraded=—
    课程列表（MySQL）      HTTP 200      2150ms  degraded=—
    RAG 检索（Milvus+图谱） HTTP 200      5276ms  degraded=有 | Milvus 检索跳过（MilvusException）；Neo4j 未连接（跳过图谱扩展）
    AI 问答（agent 全链路） HTTP 200     56903ms  degraded=—
  判定 无5xx: ✅   edu_degraded_total{component="mongo"} = 0.0
  ⚠️ mongo: edu_degraded_total 未增长（该场景可能无读路径探针命中，或该依赖本身有兜底不产生降级计数）
  降级原因样本: ['RAG 检索（Milvus+图谱）: Milvus 检索跳过（MilvusException）；Neo4j 未连接（跳过图谱扩展）']

────────────────────────────────────────────────────────────────────────
■ 场景 minio：MinIO 对象存储断连
  注入: {'MINIO_ENDPOINT': '127.0.0.1:65534'}
  期望: 上传/视频明确报错（4xx/5xx 明确，非静默），课程文字部分可用
    健康检查             HTTP 200        16ms  degraded=—
    详细健康             HTTP 200      9933ms  degraded=—
    课程列表（MySQL）      HTTP 200      2497ms  degraded=—
    RAG 检索（Milvus+图谱） HTTP 200      5872ms  degraded=有 | Milvus 检索跳过（MilvusException）；Neo4j 未连接（跳过图谱扩展）
    AI 问答（agent 全链路） HTTP 200     79127ms  degraded=—
    知识库上传（MinIO）     HTTP 200            {"task_id":"task_1787963468_f95fb2","status":"pending","message":"文件已接收，正在后台执行 p
  判定 无5xx: ✅   edu_degraded_total{component="minio"} = 0.0
  ⚠️ minio: edu_degraded_total 未增长（该场景可能无读路径探针命中，或该依赖本身有兜底不产生降级计数）
  降级原因样本: ['RAG 检索（Milvus+图谱）: Milvus 检索跳过（MilvusException）；Neo4j 未连接（跳过图谱扩展）']

========================================================================
容灾演练汇总（对照 doc-architect-tech-arch §6.4 优雅降级矩阵）
========================================================================
依赖         启动     无5xx   降级指标       期望行为
baseline   ok     ✅      0.0        稳态：所有探针 2xx，且 RAG/问答无降级原因
mongo      ok     ✅      0.0        chat_message(MySQL) 权威历史兜底，不 500
minio      ok     ✅      0.0        上传/视频明确报错（4xx/5xx 明确，非静默），课程文字部分可用