# PRD：阶段性测试与十点体验修复（v1 草案，待 owner 签收）

> 来源：owner 2026-09-22 实测反馈十条（全文见会话）+ yy 阶段 1 需求纪律。
> 原则：**一切以 owner 实测为准**——owner 说没生效就是没生效，先定因再辩护；26 条简历主张全部重测（含既往"已验证"项，不采信历史报告）。
> 签收 gate：本文档 owner 回复「确认」后进入执行；[待确认] 标记处需 owner 表态。

---

## 一、十点批评逐条处置表（定性 / 根因 / 修复 / GWT 验收）

### ① 全量重测 26 主张（含"已验证"项）
- 定性：接受。历史验收按 yy 纪律本就不该被采信为终态。
- 方案：见 §三 重测矩阵，全量执行，产出 `docs/简历实测矩阵.md`（每条：实测结果+复现命令+证据）。
- **GWT**：Given 26 条主张清单，When 矩阵执行完毕，Then 每条有「今日实测数字+可复制命令」，零条引用历史报告作为唯一证据。

### ② 流式输出 owner 多次实测未生效（P0）
- 定性：**接受，按未实现处理**。历史 CDP 实测（T7 轮）显示静态页渐进渲染，但 owner 实测从未生效=真实用户体验优先。疑点：owner 测的是 React `/chat` 还是静态 `/chat.html`？两套 chat 前端流式路径不同源。
- 修复：双端实测定因（CDP 抓 SSE 帧到达时序 vs DOM 渲染时序）→ 修复 → 以 owner 口径验收（owner 亲测看到打字机效果才算数）。
- **GWT**：Given 学生登录态，When 在 chat 页发送任意问题，Then 首字出现前有明显「检索中」指示、首字后**逐字渐进渲染**（非整段弹出）、done 后指示消失；React `/chat` 与静态 `/chat.html` 双端同验；owner 亲测 3/3 次有效。

### ③ 管理端 RAG/MCP 控制台体验差（P1）
- 定性：接受。三条子项分别处置：
  - 3a 功能说明不清晰 → 每个 tab 顶部加「本页做什么/怎么用」说明卡（3-5 行白话）
  - 3b 使用门槛高 → MCP 加「新增 Server」引导表单（分步：传输类型→命令/URL→健康测试→保存）；RAG 调参 tab 加预设说明与一键应用
  - 3c 测试遗留无效数据 → 数据审计+清理：MCP 测试 server（T14 已清 12 个，复查）、RAG 测试集合、题目库脏数据——**三核闸**清理，清理前后清单给 owner 过目
- **GWT**：Given owner 未读任何文档，When 打开 RAG/MCP 控制台任一 tab，Then 5 秒内能说出该页干什么、下一步能自己完成一次完整配置操作（不问人）。

### ④ HITL 确认卡 owner 从未成功（P0）
- 定性：**owner 说得对——根因是 `HITL_ENABLED=false`（生产默认关）**，卡片从未在真实使用中出现过；我们只在测试窗临时开过。这是"功能存在但不可达"。
- 修复提案：**dev/demo 态默认 HITL_ENABLED=true**（确认卡默认可见可用），生产切换保留配置能力；[待确认] owner 是否同意 demo 默认开。
- **GWT**：Given admin 登录 chat，When 说「帮我创建一门课程…」，Then 出现五字段确认卡（risk=high）→ 点确认→课程真实落库；点拒绝→零落库；超时不点→自动拒绝。owner 亲测 3/3。

### ⑤ OpenTelemetry 是假的（P1）
- 定性：**接受**——埋点代码全、采集栈零部署、owner 从未见过任何界面。「没用过=不存在」。
- 修复提案（二选一，[待确认]）：A) **部署 Jaeger all-in-one**（docker 一条命令）+ 配 OTEL 端点 → 真 trace 瀑布图可演示（推荐，半天）；B) 从简历删除该句。
- **GWT**：Given Jaeger 运行，When 发起一次 chat 请求，Then Jaeger UI 能看到该请求的完整 trace 瀑布（route→检索→LLM 各 span）。

### ⑥ Redis 四件套没演示方案（P1）
- 定性：接受——四件套全是后端内部机制，**没有任何用户可见面**，报告不等于演示。
- 修复：新建管理端「基础设施」页（admin-only）：限流计数（近窗 429/放行）、缓存命中演示（同参两连发：第一次 X ms/第二次 5.3ms 实时对比）、分布式锁状态、LLM 队列深度；另附「一键演示脚本」（终端实时打印四件套 live 数据）。
- **GWT**：Given admin 打开基础设施页，When 点「运行演示」，Then 页面实时展示四件套各自的真实数据变化，无一项需要读代码理解。

### ⑦ 图谱推荐路必须修复（P1）
- 定性：接受。侦察结论：`app/recommender/neo4j_engine.py` **真 Neo4j 查询代码存在但是孤儿模块**（engine.py/router.py 零引用）；推荐 API 三端点（path/next/feedback）走的是 MySQL 图（graph_node/CONTAINS）。
- 修复：把 neo4j_engine 接入推荐路由（/api/recommend/next 增加 graph 源或独立 /api/recommend/graph），配开关；MySQL 图路与 Neo4j 路并存可对照。
- **GWT**：Given 学生有学习历史，When 调推荐接口，Then 响应含 graph 源候选（Neo4j PREREQUISITE/RELATED 邻居）且带来源标注；Neo4j 停机时降级 MySQL 图路不报错。

### ⑧ MongoDB 在哪（P1 演示面）
- 定性：Mongo 在 VM 运行且真实在用（`edu_agent` 库：artifacts.chunks/files + learning_event），但**零可见面**——owner 没用过很正常。
- 修复：并入 §⑥ 基础设施页（Mongo 集合统计：learning_event 事件数/最近事件样例/artifacts 存储量），演示时讲清"哪个功能的数据住在 Mongo"。
- **GWT**：Given admin 打开基础设施页，Then 能看到 Mongo 三个集合的实时计数与最近一条 learning_event 内容。

### ⑨ AI Agent 协作工程自证包（P1）
- 定性：接受——协作工程是我的主张，必须给 owner 可独立验证的路径。
- 修复：产出 `docs/agent-collab-verify.md`：owner 按步骤自验（①queue.md 状态表对照 git log 逐条核 SHA ②reports/ 目录 15 份完工报告 ③盲测队列 B1-B9 与结果 ④一键复核命令集：pytest 面/五门/关键 curl）+ 一个 `verify-auto20.cmd` 一键复核脚本。
- **GWT**：Given owner 打开该文档，When 照做 ≤15 分钟，Then 亲自复现「17 任务交付」的核心证据（不依赖我的任何转述）。

### ⑩ 「AI 谎报黄条」是假的（P0）
- 定性：**接受**——后端字段已验（tool_receipt_unverified=True 实测），但**前端黄条从未在真实触发场景下被视觉验证过**（T7 只验了 G3 语法门）。owner 说假就是假。
- 修复：CDP 视觉实证——构造稳定触发场景（诱导调用不可用工具）→ 截图黄条真实渲染；若 UI 渲染路径真断→修复。同时把触发话术写进演示手册（保证演示时 100% 可复现）。
- **GWT**：Given 学生账号，When 输入手册指定的触发话术，Then 气泡尾部黄色「⚠️ 未实际执行」条**视觉可见**（截图三连：无护栏前/后/多分辨率）。

---

## 二、突发新发现（本轮侦察实测）

- **N1 后端再次无声死亡**（owner 反馈期间实测 health=000，已手工拉起）——看门狗移除后的真实风险。提案：start 脚本内置可选轻守护循环（后台轮询 health，挂了自动重启+写日志）[待确认] 或接受人工一键拉起。
- **N2 推荐系统孤儿模块**：neo4j_engine.py 未接线（见⑦）。
- **N3 KG_EXPAND 已开启**：图谱第四通道返回实体实测（graph_entities=12），chat 响应可查。

---

## 三、全量重测矩阵（26 主张，执行阶段产出 `docs/简历实测矩阵.md`）

| 层 | 覆盖 | 方法 |
|---|---|---|
| 学生端 10 项 | 引用溯源/三路融合歧义消解/LLM 抽取/事件溯源/召回加权/多轮/分级课程/Quiz/SM-2 定位/社区成就 | CDP 行为实测+代码定位+数据抽查 |
| 管理端 4 项 | 课程/题库/用户管理、RAG 控制台、MCP 工具链、HITL | CDP+真实 HTTP |
| RAG 链 4 项 | BGE-M3 双向量/hybrid+RRF/sidecar 3.6x/万级题库 | 检索日志取证+报告复核+sidecar 实起对照 |
| Redis/可观测 6 项 | 限流/缓存/削峰/锁/冷启动 2.7s/P95 6.4s/OTel/部署 | 脚本复跑+新基础设施页+Jaeger |

---

## 四、分期与工作量

| 期 | 内容 | 预估 |
|---|---|---|
| **P0 演示阻断** | ②流式 UI 定因修复 / ④HITL 默认开+UI 实弹 / ⑩黄条视觉实证 / ③c 遗留数据清理 / N1 守护方案 | 1 天 |
| **P1 体验+真实性** | ⑦图谱推荐接线 / ⑤OTel+Jaeger / ⑥⑧基础设施页 / ③ab 控制台 UX / ⑨自证包 | 1-2 天 |
| **P2 全量重测** | 26 主张矩阵+简历实测矩阵.md | 0.5 天 |

## 五、[待确认] 清单（owner 表态即开工）

1. HITL demo 默认开（④）——是/否
2. OTel 走 Jaeger 部署（⑤A）还是删简历句（⑤B）
3. 后端守护：轻守护循环（N1）还是人工一键拉起
4. 图谱推荐修复后，简历"三路融合"措辞是否改为与实现一致

---

## 六、纪律声明

- 本 PRD 全部根因判定基于今日实测（可复现命令在会话与报告）；owner 十条反馈无一辩护，全部转入修复或如实登记。
- 执行期纪律不变：C-01 逐断言验收、批判登记、盲测队列更新（新增 B10 流式 UI/B11 黄条视觉/B12 HITL 卡三场景）。
