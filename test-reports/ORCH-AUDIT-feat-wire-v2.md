# 编排者批判审计报告：FEAT-WIRE-V2（2026-09-21，用户令：至少 5 个 P0，必须修新页面）

> 验收结论：**有条件通过 + 返工**。12 缺陷中 10 条三段式证据我抽查复证成立（#1/#3/#5/#6/#7/#8/#9/#12 亲证或强证据）；#11 为**半修复**（P0-1/P0-2）；工作流 B 的 dead=0 存在口径漏洞（P0-3）。186 passed 我亲跑复证。

## P0 清单（6 个，全部新页面/新链路）

### P0-1 跨会话记忆召回选错事实（#11 残留——用户原缺陷体感依旧）
**实证**：A 会话自述「我叫审测员777」→ 事件表 id=211/212 **正确落库**；立刻开 B 会话问「我叫什么」→ 答「fw2小明5956」；**45 秒后再问 → 答「fw2小明1073」**（既不是最新也不是次新）。
**根因方向**：召回按相关度排序，最新事实无优先/无冲突消解；同账号多条名字记忆并存时随机赢。
**修法锚点**：召回注入处（answer 节点轻量召回）+ user_memory HEAD 判据（AGENTS 教训 11：valid_to IS NULL AND event_type<>'delete'）——最新 create 事件的同槽位事实应压过旧记忆。

### P0-2 记忆提取 ~45s 延迟 + 零用户反馈（#11 残留另一半）
**实证**：自述后立刻问=答错；45s 后仍错（叠加 P0-1）。chat 页无「已记住」任何提示。
**用户体感**=「记忆功能没实现」（用户原话）。40s「最终一致」对演示是灾难：观众说完名字马上换会话问，必翻车。
**修法锚点**：① 提取前置（answer 完成即同步提取，或 memory 子代理管线内联）② chat 页答后落「💾 已记住你的名字」反馈条（有记忆事件落库时）。

### P0-3 dead=0 口径漏洞：83 个 delegated 元素未做点击差分
**实证**：wiring-matrix.json = 601 wired + **83 delegated**；执行者自披露「delegated 未做全量点击差分」。历史 12 缺陷中 #2/#3/#6 全是「有点击绑定但 handler 坏」=delegated 形态。
**含义**：「全站 dead=0」真实语义=「无零绑定元素」，**不等于「全站点得动」**。83 个盲区=下一批用户实测缺陷的温床。
**修法锚点**：对 83 个 delegated 元素逐一 CDP 真实点击差分（点击前后 DOM/网络差），坏 handler 当场修——这是 dead=0 声明补全的唯一路径。

### P0-4 视频上传仅 2.3MB 冒烟，真实课程文件（100MB-1GB）路径未验
**实证**：执行者只传了 2.3MB 测试片。分片管线（init→chunk→finalize→bind→transcode）在，但**分片超时、断点续传、失败重试、进度错误恢复零实测**；页面 15 处 catch 存在≠路径被验。
**风险**：用户真实使用/面试演示传大文件时翻车概率高（首片失败/中途断网无恢复 UI）。
**修法锚点**：①≥100MB 文件全链实测（可用 ffmpeg 现生成）②失败注入测试（中途 kill 一片→UI 呈现可重试态）③进度/错误态诚实呈现。

### P0-5 #8 隔离修复的运营代价：管理端会话审计能力归零
**实证**：隔离亲证正确（admin 只见 100003、student 只见 1）——但代价是 admin **完全无法**查看学员会话（旧全局分支被废除，无替代视图）。教育平台的运营/内容审核场景（巡检学员对话质量、违规内容）现在没有入口。
**修法锚点**：管理端新增「会话审计」只读视图（admin-only 端点 + admin-chat-audit 页面，明确标注审计用途）——恢复能力但走显式授权面而非隐式全局分支。

### P0-6 演示主账号记忆污染无治理
**实证**：user000001 名下 4+ 条冲突名字记忆并存（「什么名字」/fw2小明5956/fw2小明1073/审测员777），无 update 语义、无清理工具。面试演示时 AI 引用哪条全看召回运气——**与 P0-1 叠加=演示翻车直连项**。
**修法锚点**：①名字槽位 update 语义（新名字 create 事件应触发旧名字 valid_to 关闭——事件溯源支持，AGENTS 教训 11 HEAD 判据已备）②一次性治理脚本清演示账号冲突记忆（三核闸）。

## 非 P0 登记
- HITL_ENABLED=False 生产默认=knowledge_import 等 admin_write 工具零确认直执行（配置态非缺陷，但演示语义与"写类工具须确认"叙事矛盾——建议演示时开）
- #2 用户原话含「返回按钮」：doBack() 固定路由跳转语义正确（实查），此项不立缺陷
- Mimosa scanner_enobufs 持续 fail-open 窗口（多轮未出完整结论）——全量审计重跑仍排程中
